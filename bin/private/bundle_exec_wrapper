#!/bin/bash
#
# Copyright (c) 2019 Jeremy Green.

set -o errexit
set -o nounset
set -o pipefail
set -o errtrace

wrapperDir=$(dirname "$(readlink -e "$0")")
# shellcheck source=bin/.common.sh
source "${wrapperDir}/../.common.sh"
# shellcheck source=environment
source "${wrapperDir}/../../environment"
exec bundler exec "$(basename "$0")" "$@"
